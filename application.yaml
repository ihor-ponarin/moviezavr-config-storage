server:
  port: 8000

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/h2_db/moviezavrdb
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update